apiVersion: v1
kind: Namespace
metadata:
  name: "{{ k8s_namespace }}"
  labels:
    # Reject pods that mount host paths, run privileged, or use host
    # networking; warn about what the stricter "restricted" level needs.
    pod-security.kubernetes.io/enforce: baseline
    pod-security.kubernetes.io/warn: restricted
